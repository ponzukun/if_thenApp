<% provide(:title, 'All users') %>
<% provide(:path,  users_path) %>
<h1>All users</h1>

<!-- 検索機能 -->
<%= render 'users/search' %>

<%= will_paginate %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>
