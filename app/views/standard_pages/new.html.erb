<% provide(:title, 'New') %>
<% provide(:path,  new_path) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <%= render 'shared/user_info' %>
    </section>
    <section class="ifthen_rule_form">
      <%= render 'shared/ifthen_rule_form' %>
    </section>
  </aside>
  <div class="col-md-8">
    <div class="search-form">
      <h3>IfthenRule(<%= current_user.ifthen_rules.count %>)</h3>
      <%= render 'shared/search' %>
    </div>
    <%= render 'shared/feed' %>
  </div>
</div>
